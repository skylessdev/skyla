Perfect—here’s a tight Replit agent prompt that will actually unify the header styling across all pages, using the homepage look and your skyla2.png logo.

⸻

Replit agent prompt

Goal: Make the header identical on /, /demo, /docs, /sign-in, using the homepage’s spacing/weight/size and the public/assets/skyla2.png logo. Remove any page-specific header CSS.

Do exactly this:
	1.	Create/overwrite public/styles/site.css:

:root{--beige:#f5f4f0;--ink:#0f0f0f;--muted:#666}
.site-header{
  display:flex;align-items:center;justify-content:space-between;
  padding:24px 20px;background:var(--beige);max-width:1160px;margin:0 auto
}
.site-brand{display:flex;align-items:center;gap:10px}
.site-brand img{height:28px;width:auto;display:block}
.site-nav{display:flex;gap:32px}
.site-nav a{
  color:var(--ink);text-decoration:none;font-size:15px;font-weight:450;
  letter-spacing:.01em;opacity:.9
}
.site-nav a:hover{opacity:1}

	2.	Replace views/partials/header.ejs with:

<link rel="stylesheet" href="/styles/site.css">
<header class="site-header">
  <a class="site-brand" href="/"><img src="/assets/skyla2.png" alt="Skyla"/></a>
  <nav class="site-nav" aria-label="Main">
    <a href="/demo">Demo</a>
    <a href="/docs">Docs</a>
    <a href="/sign-in">Sign in</a>
  </nav>
</header>

	3.	In each of views/index.ejs, views/demo.ejs, views/docs.ejs, views/sign-in.ejs:

	•	Ensure the header is only: <%- include('partials/header') %> (near the top).
	•	Remove any page-level CSS that targets header/nav/links (e.g., classes like .header, .brand, .nav, .top, or inline <style> blocks affecting header).
	•	Do not change console panel fonts/logic in demo.ejs.

	4.	Confirm static assets and routes:

	•	server.js includes app.use(express.static(path.join(__dirname,'public')));
	•	public/assets/skyla2.png exists and loads.

	5.	QA (report success + files changed):

	•	All four routes render the same header: logo left, “Demo · Docs · Sign in” right.
	•	Link color/weight/size/spacing match homepage.
	•	No remaining duplicate header CSS.

⸻
