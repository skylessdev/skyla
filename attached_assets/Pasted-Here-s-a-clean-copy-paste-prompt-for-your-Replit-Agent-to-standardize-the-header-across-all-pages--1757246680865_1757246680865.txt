Here’s a clean, copy-paste prompt for your Replit Agent to standardize the header across all pages (use the current homepage header) and remove every instance of the image logo.

⸻

🔧 Replit Agent Task — Unify Header (keep homepage style) & Remove Logo

Goal
	•	Use the homepage header (text wordmark skyla.dev on the left; right-aligned links) as the single, consistent header on all pages.
	•	Remove all image logos (e.g., skyla-mark.png, any .svg/.png marks) from every page.
	•	Keep routes and behavior unchanged.

Files to edit
	•	public/index.html
	•	public/demo.html
	•	public/docs.html
	•	public/sign-in.html
	•	(Optional cleanup) public/assets/skyla-mark.* and any other logo image files

⸻

1) Create a tiny shared header snippet (inline-friendly)

Add this exact header markup and CSS to each page (replace their current header/nav).
Use this CSS block (place in each page’s <style> after existing variables, or at the end):

<style>
/* === Unified header (homepage style) === */
.s-header{display:flex;align-items:center;justify-content:space-between;margin:0 0 28px 0}
.s-brand{display:flex;align-items:center;gap:10px;font-weight:600;letter-spacing:.2px}
.s-brand__dot{display:inline-block;width:18px;height:18px;border:1.5px solid #111;border-radius:4px;position:relative;background:#fff}
.s-brand__dot::after{content:"";position:absolute;top:-4px;left:50%;transform:translateX(-50%);width:6px;height:6px;background:#111;border-radius:50%}
.s-brand span{font-weight:600}
.s-nav a{color:#0f0f0f;text-decoration:none;margin-left:22px;font-size:14px}
.s-nav a:hover{text-decoration:underline}
@media (max-width:700px){
  .s-nav a{margin-left:16px}
}
</style>

Use this HTML header (drop inside <body> container, at the top on each page):

<header class="s-header">
  <div class="s-brand">
    <i class="s-brand__dot" aria-hidden="true"></i>
    <span>skyla.dev</span>
  </div>
  <nav class="s-nav">
    <a href="/demo">Demo</a>
    <a href="/docs">Docs</a>
    <a href="/sign-in">Sign in</a>
  </nav>
</header>

Notes
• This matches the homepage look but uses a tiny CSS-drawn mark (no image).
• If the homepage already has an equivalent header, still replace it with this exact snippet to guarantee consistency.

⸻

2) Remove all image logo usage

For each file, delete any of the following (and their surrounding containers if they become empty):
	•	<img ... skyla-mark…
	•	<img ... alt="Skyla mark"…
	•	Any <div class="logo">…</div> blocks or custom logo wrappers
	•	Any <img src="/assets/*.png"> or .svg that is a brand icon

Specifics:
	•	public/demo.html: remove the logo image above the “Skyla” heading; keep the rest of the console UI.
If there’s a centered <img class="logo-img" ...>, delete that line and its styles (e.g., .logo-img rules).
	•	public/index.html: if an image logo exists in the header, remove it. Use the new header snippet only.
	•	public/docs.html & public/sign-in.html: replace their top bars with the unified header and delete any logo <img> tags.

⸻

3) Optional asset cleanup
	•	Delete unused logo files to avoid accidental reuse:
	•	public/assets/skyla-mark.png (and any variants)
	•	Any other brand mark images (logo.svg, mark.svg, etc.)

(If other pages reference them for non-header content, leave them; otherwise remove.)

⸻

4) Make sure spacing looks identical on all pages
	•	After inserting the unified header, ensure the main page containers don’t add a duplicate top margin.
If a page looks too tight, add a small spacer after the header:

<div style="height:8px"></div>

(Use only if needed.)

⸻

5) Acceptance criteria
	•	Header across /, /demo, /docs, /sign-in matches homepage style (text skyla.dev left; links right; no image logo).
	•	No <img> logo elements remain in any of the four files.
	•	Navigational links work and routes unchanged.
	•	Visual regression: hero and proof card remain exactly as before on /.
	•	No 404s for deleted assets.

⸻

6) Quick test plan
	1.	Open / → See unified header; no image logo.
	2.	Open /demo → Header identical to /; no logo above console.
	3.	Open /docs and /sign-in → Same header; no logos.
	4.	Browser dev tools: search for skyla-mark and <img with alt="Skyla mark" → none present (except non-brand content if any).
	5.	Console: no 404s for removed assets.

⸻

If anything is unclear, keep all functional code intact and only change header + logo removal as described.